<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="../../../../../../../dev/tests/acceptance/vendor/magento/magento2-functional-testing-framework/src/Magento/FunctionalTestingFramework/Test/etc/testSchema.xsd">
    <test name="AddNewBundleProductTest">
        <annotations>
            <features value="Add product to cart"/>
            <stories value="Add bundle product to cart"/>
            <title value="Add bundle product to cart"/>
            <description value="Add a bundle product which is not added to cart yet before"/>
            <severity value="CRITICAL"/>
            <testCaseId value="ABP-01"/>
            <group value="addNewBundleToCart"/>
            <group value="addBundleProduct"/>
            <group value="pwapos"/>
        </annotations>

        <before>
            <actionGroup ref="PosOnCheckoutPage" stepKey="PosOnCheckoutPage"/>
        </before>

        <actionGroup ref="ClearCart" stepKey="ClearCart" />

        <actionGroup ref="SearchAndClickProduct" stepKey="searchAndClickGroupedProduct">
            <argument name="dataProduct" value="pos_bundle_product"/>
        </actionGroup>

        <waitForElementVisible selector="{{PosCheckoutPopupBundleProductSection.popupBundleBlock}}" time="60" stepKey="waitPopupBundleVisible"/>
        <waitForElementNotVisible selector="{{PosCheckoutPopupBundleProductSection.loaderOption}}" time="60" stepKey="waitForLoadOption"/>
        <wait time="3" stepKey="waitToAdd"/>

        <click selector="{{PosCheckoutPopupBundleProductSection.product1}}" stepKey="chooseOptionProduct1"/>
        <click selector="{{PosCheckoutPopupBundleProductSection.product2}}" stepKey="chooseOptionProduct2"/>

        <click selector="{{PosCheckoutPopupBundleProductSection.addToCart}}" stepKey="clickAddButton"/>
        <waitForElementVisible selector="{{CartSection.productFoundOnCartByName(pos_bundle_product.name)}}" time="60" stepKey="waitForItemVisibleInCart"/>
        <seeElement selector="{{CartSection.productFoundOnCartByName(pos_bundle_product.name)}}" stepKey="seeItemInCart"/>
    </test>

    <test name="AddExistedBundleProductTest">
        <annotations>
            <features value="Add product to cart"/>
            <stories value="Add bundle product to cart"/>
            <title value="Add more bundle product which existed in cart"/>
            <description value="Add more bundle product which existed in cart"/>
            <severity value="CRITICAL"/>
            <testCaseId value="ABP-02"/>
            <group value="addExistedBundleToCart"/>
            <group value="addBundleProduct"/>
            <group value="pwapos"/>
            <group value="demo"/>
        </annotations>

        <before>
            <actionGroup ref="PosOnCheckoutPage" stepKey="PosOnCheckoutPage"/>
        </before>

        <actionGroup ref="ClearCart" stepKey="ClearCart" />

        <actionGroup ref="SearchAndClickProduct" stepKey="searchAndClickGroupedProduct">
            <argument name="dataProduct" value="pos_bundle_product"/>
        </actionGroup>

        <waitForElementVisible selector="{{PosCheckoutPopupBundleProductSection.popupBundleBlock}}" time="60" stepKey="waitPopupBundleVisible1"/>
        <waitForElementVisible selector="{{PosCheckoutPopupBundleProductSection.product1}}" time="60" stepKey="waitForLoadOption1"/>
        <waitForElementVisible selector="{{PosCheckoutPopupBundleProductSection.product2}}" time="60" stepKey="waitForLoadOption2"/>
        <wait time="4" stepKey="waitToAdd1"/>

        <click selector="{{PosCheckoutPopupBundleProductSection.product1}}" stepKey="chooseOptionProduct1"/>
        <click selector="{{PosCheckoutPopupBundleProductSection.product2}}" stepKey="chooseOptionProduct2"/>

        <click selector="{{PosCheckoutPopupBundleProductSection.addToCart}}" stepKey="clickAddButton1"/>
        <waitForElementNotVisible selector="{{PosCheckoutPopupBundleProductSection.popupBundleBlock}}" time="60" stepKey="waitForPopupClosed1"/>
        <waitForElementVisible selector="{{CartSection.productFoundOnCartByName(pos_bundle_product.name)}}" time="60" stepKey="waitForItemVisibleInCart"/>
        <seeElement selector="{{CartSection.productFoundOnCartByName(pos_bundle_product.name)}}" stepKey="seeItemInCart"/>

        <click stepKey="clickOnProductFoundInList2" selector="{{ProductListSection.firstProduct}}"/>
        <waitForElementVisible selector="{{PosCheckoutPopupBundleProductSection.popupBundleBlock}}" time="60" stepKey="waitPopupBundleVisible2"/>
        <waitForElementVisible selector="{{PosCheckoutPopupBundleProductSection.product1}}" time="60" stepKey="waitForLoadOption1Time2"/>
        <waitForElementVisible selector="{{PosCheckoutPopupBundleProductSection.product2}}" time="60" stepKey="waitForLoadOption2Time2"/>
        <wait time="4" stepKey="waitToAdd2"/>

        <click selector="{{PosCheckoutPopupBundleProductSection.product1}}" stepKey="chooseOptionProductDoubleTime1"/>
        <click selector="{{PosCheckoutPopupBundleProductSection.product2}}" stepKey="chooseOptionProductDoubleTime2"/>

        <click selector="{{PosCheckoutPopupBundleProductSection.addToCart}}" stepKey="clickAddButton2"/>
        <waitForElementNotVisible selector="{{PosCheckoutPopupBundleProductSection.popupBundleBlock}}" time="60" stepKey="waitForPopupClosed2"/>

        <grabTextFrom selector="{{CartSection.productQtyInCart(pos_bundle_product.name)}}" stepKey="grabItemQty"/>

        <assertEquals message="Assert item qty is correct" stepKey="assertItemQtyIsCorrect">
            <expectedResult type="float">2</expectedResult>
            <actualResult type="variable">grabItemQty</actualResult>
        </assertEquals>
    </test>
</tests>
